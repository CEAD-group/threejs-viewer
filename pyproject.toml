[project]
name = "threejs-viewer"
version = "0.0.0-dev"
description = "Lightweight Three.js viewer controlled from Python via WebSocket"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Thijs Damsma" }
]
keywords = ["three.js", "3d", "visualization", "robotics", "websocket", "viewer"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Visualization",
    "Topic :: Multimedia :: Graphics :: 3D Rendering",
]
dependencies = [
    "numpy>=1.24.0",
    "websockets>=12.0",
]

[project.urls]
Homepage = "https://github.com/thijsdamsma/threejs-viewer"
Repository = "https://github.com/thijsdamsma/threejs-viewer"

[project.scripts]
threejs-viewer = "threejs_viewer.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/threejs_viewer"]
# Include non-Python files (viewer.html)
artifacts = ["src/threejs_viewer/viewer.html"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/README.md",
    "/DESIGN.md",
    "/THIRD_PARTY_NOTICES.md",
]

# Ship the third-party notices inside the wheel too (Apache-2.0 §4(a)/(d) for
# the embedded turbo colormap data).
[tool.hatch.build.targets.wheel.force-include]
"THIRD_PARTY_NOTICES.md" = "threejs_viewer/THIRD_PARTY_NOTICES.md"

# Force include viewer.html in wheel (it's not a .py file)
[tool.hatch.build]
include = [
    "src/threejs_viewer/*.py",
    "src/threejs_viewer/viewer.html",
]

[dependency-groups]
dev = [
    "pytest>=7.0",
    "ruff>=0.1.0",
]
browser-test = [
    "pytest-playwright>=0.6.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "browser: integration tests requiring Playwright (headless Chromium)",
]
